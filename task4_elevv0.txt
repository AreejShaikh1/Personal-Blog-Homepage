<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Personal Blog - Sukkur, Pakistan</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background: #f8fafc;
      color: #1e293b;
    }

    header {
      background: #0f172a;
      color: #fff;
      text-align: center;
      padding: 2rem 1rem;
    }

    header h1 {
      margin: 0;
      font-size: 2.2rem;
    }

    header p {
      margin: 0.5rem 0 0;
      font-size: 1.1rem;
      opacity: 0.8;
    }

    .controls {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 10px;
      margin: 1rem;
    }

    .controls select,
    .controls input {
      padding: 0.5rem;
      font-size: 1rem;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.5rem;
      padding: 1.5rem;
    }

    .card {
      background: #fff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 14px rgba(0,0,0,0.15);
    }

    .card img {
      width: 100%;
      height: 160px;
      object-fit: cover;
    }

    .card-content {
      padding: 1rem;
    }

    .card-content h3 {
      margin: 0 0 0.5rem;
      font-size: 1.2rem;
      color: #0f172a;
    }

    .card-content p {
      margin: 0 0 0.5rem;
      font-size: 0.95rem;
      color: #475569;
    }

    .card-content small {
      color: #64748b;
      font-size: 0.85rem;
    }

    .pagination {
      text-align: center;
      margin: 1rem 0;
    }

    .pagination button {
      padding: 0.6rem 1rem;
      margin: 0 5px;
      border: none;
      background: #0f172a;
      color: #fff;
      border-radius: 8px;
      cursor: pointer;
      font-size: 0.95rem;
      transition: background 0.2s;
    }

    .pagination button:hover {
      background: #1e293b;
    }

    footer {
      text-align: center;
      padding: 1rem;
      background: #0f172a;
      color: #fff;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>My Personal Blog</h1>
    <p>Welcome to my blog from Sukkur, Pakistan 🌍</p>
  </header>

  <div class="controls">
    <select id="categoryFilter">
      <option value="All">All Categories</option>
      <option value="Tech">Tech</option>
      <option value="Travel">Travel</option>
      <option value="Food">Food</option>
    </select>
    <input type="text" id="searchInput" placeholder="Search posts by title..." />
  </div>

  <div class="grid" id="postsGrid"></div>

  <div class="pagination">
    <button id="prevPage">Previous</button>
    <button id="nextPage">Next</button>
  </div>

  <footer>
    <p>© 2025 My Blog | Sukkur, Pakistan</p>
  </footer>

  <script>
    const posts = [
      { title: "Tech in 2025", category: "Tech", image: "https://via.placeholder.com/400x200?text=Tech", description: "Exploring the future of technology.", date: "2025-08-01" },
      { title: "My Trip to Karachi", category: "Travel", image: "https://via.placeholder.com/400x200?text=Travel", description: "A wonderful journey to Karachi.", date: "2025-07-15" },
      { title: "Best Biryani in Sindh", category: "Food", image: "https://via.placeholder.com/400x200?text=Food", description: "Tasting the best biryani in Sukkur.", date: "2025-06-25" },
      { title: "AI & Future Jobs", category: "Tech", image: "https://via.placeholder.com/400x200?text=AI", description: "How AI will impact careers.", date: "2025-05-10" },
      { title: "Trip to Islamabad", category: "Travel", image: "https://via.placeholder.com/400x200?text=Islamabad", description: "Exploring Islamabad’s beauty.", date: "2025-04-20" },
      { title: "Street Food in Karachi", category: "Food", image: "https://via.placeholder.com/400x200?text=Foodie", description: "Street food adventures in Karachi.", date: "2025-03-12" },
    ];

    const postsPerPage = 3;
    let currentPage = 1;

    const postsGrid = document.getElementById("postsGrid");
    const categoryFilter = document.getElementById("categoryFilter");
    const searchInput = document.getElementById("searchInput");
    const prevPageBtn = document.getElementById("prevPage");
    const nextPageBtn = document.getElementById("nextPage");

    function renderPosts() {
      postsGrid.innerHTML = "";
      let filteredPosts = posts.filter(post =>
        (categoryFilter.value === "All" || post.category === categoryFilter.value) &&
        post.title.toLowerCase().includes(searchInput.value.toLowerCase())
      );

      const start = (currentPage - 1) * postsPerPage;
      const end = start + postsPerPage;
      const paginatedPosts = filteredPosts.slice(start, end);

      paginatedPosts.forEach(post => {
        const card = document.createElement("div");
        card.className = "card";
        card.innerHTML = `
          <img src="${post.image}" alt="${post.title}" />
          <div class="card-content">
            <h3>${post.title}</h3>
            <p>${post.description}</p>
            <small>${post.date} | ${post.category}</small>
          </div>
        `;
        postsGrid.appendChild(card);
      });

      prevPageBtn.disabled = currentPage === 1;
      nextPageBtn.disabled = end >= filteredPosts.length;
    }

    categoryFilter.addEventListener("change", () => {
      currentPage = 1;
      renderPosts();
    });

    searchInput.addEventListener("input", () => {
      currentPage = 1;
      renderPosts();
    });

    prevPageBtn.addEventListener("click", () => {
      if (currentPage > 1) {
        currentPage--;
        renderPosts();
      }
    });

    nextPageBtn.addEventListener("click", () => {
      currentPage++;
      renderPosts();
    });

    renderPosts();
  </script>
</body>
</html>
